# Deprecated experimental folder

This folder is deprecated.

Runtime RAG logic has been migrated to `services/ai-agent/app/tools/rag.py`.
Runtime RAG data has been migrated to `services/ai-agent/app/data-rag/`.

Keep this folder only as a temporary migration fallback. It should be removed
after ai-agent RAG tests and smoke checks are fully validated.
